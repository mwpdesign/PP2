# Healthcare IVR Platform - Local Development Environment Configuration
# Copy this file to .env and adjust values as needed

# Database Configuration (PostgreSQL in Docker)
DB_HOST=localhost
DB_PORT=5432
DB_NAME=healthcare_ivr
DB_USER=postgres
DB_PASSWORD=password

# Full Database URL (this will override individual DB_* variables if set)
DATABASE_URL=postgresql+asyncpg://postgres:password@localhost:5432/healthcare_ivr

# JWT Configuration
SECRET_KEY=your-super-secret-jwt-key-change-this-in-production-please-make-it-long-and-random
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=10080

# Authentication Mode
AUTH_MODE=local
USE_COGNITO=false

# Application Settings
DEBUG=true
ENVIRONMENT=development

# CORS Origins (for frontend development)
BACKEND_CORS_ORIGINS=["http://localhost:3000", "http://127.0.0.1:3000"] 